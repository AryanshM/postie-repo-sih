import { View, Text } from "react-native";
import React from "react";
import MyProfile from "@/app/(home-screen)/(hpo)/(services)/(profile)/MyProfile";
const MyProfilePage = () => {
  return <MyProfile />;
};

export default MyProfilePage;
