import { View, Text } from "react-native";
import React from "react";
import ArticleDetails from "../(services)/(book-consigmnet)/ArticleDetails";
const CreatePackage = () => {
  return (
    <View className="flex-1">
      <ArticleDetails />
    </View>
  );
};

export default CreatePackage;
